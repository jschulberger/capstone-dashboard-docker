version: '2'
services:
  frontend:
    build:
      context: frontend
      dockerfile: Dockerfile
    expose:
      - "4200"
  display:
    build:
      context: display
      dockerfile: Dockerfile
    depends_on:
      - frontend
    links:
      - "frontend:fe"
    environment:
      - WPE_BCMRPI_CURSOR=1
      - WPE_URL="fe:4200"
